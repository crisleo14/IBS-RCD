@model Accounting_System.Models.CollectionReceiptBook

@{
    ViewData["Title"] = "Collection Receipt Book";
    var dateFrom = ViewBag.DateFrom;
    var dateTo = ViewBag.DateTo;
}

<h1>@ViewData["Title"]</h1>

<partial name="_Notification">

    <form asp-action="CollectionReceiptBookReport" method="post">
        <div class="row">
            <div class="col-md-4">
                <div class="mb-3">
                    <label asp-for="DateFrom" class="control-label"></label>
                    <input type="date" asp-for="DateFrom" class="form-control" value="@dateFrom" />
                    <span asp-validation-for="DateFrom" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="DateTo" class="control-label"></label>
                    <input type="date" asp-for="DateTo" class="form-control" value="@dateTo" />
                    <span asp-validation-for="DateTo" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <button type="submit" class="btn-primary">Generate</button>
                </div>
            </div>
        </div>
    </form>

    @section Scripts {
        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
    }
