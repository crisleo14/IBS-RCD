@using Accounting_System.Models.ViewModels
@model ViewModelBook

@{
ViewData["Title"] = "Inventory Book";
var dateFrom = ViewBag.DateFrom;
var dateTo = ViewBag.DateTo;
}

<h1>@ViewData["Title"]</h1>
<hr />

<form id="dynamicForm" method="post">
    <div class="row">
        <div class="col-md-4">
            <div class="mb-3">
                <label class="control-label">As Of</label>
                <input type="date" asp-for="DateTo" class="form-control" value="@DateTime.Now.ToString("yyyy-MM-dd")" />
                <span asp-validation-for="DateTo" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <button type="button" class="btn btn-primary" onclick="submitForm('InventoryBookReport')"><i class="bi bi-printer"></i> Generate</button>
                <button type="button" class="btn btn-primary" onclick="submitForm('GenerateInventoryBookTxtFile')"><i class="bi bi-file-text"></i> Generate as .txt file</button>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        function submitForm(action) {
            var form = document.getElementById('dynamicForm');
            form.action = action;
            form.submit();
        }
    </script>
}
