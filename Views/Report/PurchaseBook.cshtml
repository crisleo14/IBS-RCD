@model Accounting_System.Models.ViewModelBook

@{
    ViewData["Title"] = "Purchase Book";
    var dateFrom = ViewBag.DateFrom;
    var dateTo = ViewBag.DateTo;
}

<h1>@ViewData["Title"]</h1>

<partial name="_Notification">

    <form asp-action="PurchaseBookReport" method="post">
        <div class="row">
                <div class="col-md-4">
                <div class="mb-3">
                    <label asp-for="DateFrom" class="control-label"></label>
                    <input type="date" asp-for="DateFrom" class="form-control" value="@dateFrom" />
                    <span asp-validation-for="DateFrom" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="DateTo" class="control-label"></label>
                    <input type="date" asp-for="DateTo" class="form-control" value="@dateTo" />
                    <span asp-validation-for="DateTo" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label class="control-label">Select type of document(Optional)</label>
                    <select name="selectedFiltering" class="form-control js-select2">
                        <option value="" selected="Disabled">--Select an option--</option>
                        <option value="DueDate">Due Date</option>
                        <option value="UnpostedRR">Unposted RR (Show Quantity)</option>
                        <option value="POLiquidation">PO Liquidation (Based on Due Date)</option>
                    </select>
                </div>
                </div>

                <div class="col-md-4">
                <div class="form-group mb-3">
                    <label class="control-label">PO Liquidation Per PO</label>
                    <select name="poListFrom" class="form-control js-select2" asp-items="@Model.PO">
                        <option></option>
                    </select>
                </div>
                <div class="form-group mb-3">
                    <select name="poListTo" class="form-control js-select2" asp-items="@Model.PO">
                        <option></option>
                    </select>
                </div>
                </div>
                <div class="mb-3">
                    <button type="submit" class="btn-primary">Generate</button>
                </div>
        </div>
    </form>

    @section Scripts {
        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
    }

